import React from "react";
import { storiesOf } from "@storybook/react";

import Button from "./StorybookIcon";

storiesOf("Basics|Brand/StorybookIcon", module).add("default", () => (
  <Button>Button</Button>
));
